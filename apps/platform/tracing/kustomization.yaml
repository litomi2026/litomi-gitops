apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: tracing

generatorOptions:
  disableNameSuffixHash: true

resources:
  - namespace.yaml
  - serviceaccount-eso-vault.yaml
  - networkpolicy-collector-ingress.yaml
  - networkpolicy-tempo-ingress.yaml

configMapGenerator:
  - name: vault-ca
    files:
      - ca.crt=../../../bootstrap/pki/vault-ca.pem
