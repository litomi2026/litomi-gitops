# Cache-only Redis (persistence disabled)
save ""
appendonly no

# Memory guardrail: keep this below the container memory limit to avoid OOMKills.
# Pod limit: 256Mi -> maxmemory: 192mb
maxmemory 192mb
maxmemory-policy allkeys-lru
maxmemory-samples 10

# Networking
bind 0.0.0.0
tcp-keepalive 60
