apiVersion: secrets.litomi.io/v1alpha1
kind: VaultSecretTemplate
spec:
  vaultPath: clusters/stg-01/cloudflared/cloudflared-token
  owner: stg-01/litomi/public-edge/cloudflared
  consumerFiles:
    - clusters/stg-01/litomi/public-edge/cloudflared/externalsecret-cloudflared-token.yaml
  allowAdditionalKeys: false
  requiredKeys:
    - token
  placeholderPatterns:
    - '^replace-with-cloudflared-token$'
