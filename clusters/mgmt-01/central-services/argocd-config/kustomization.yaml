apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

generatorOptions:
  disableNameSuffixHash: true

resources:
  - ../../../../apps/platform/argocd-config
  - secretstore-vault.yaml
  - externalsecret-github-repo-creds.yaml
  - ingress.yaml

configMapGenerator:
  - name: vault-ca
    files:
      - ca.crt=../../../../bootstrap/pki/vault-ca.pem
