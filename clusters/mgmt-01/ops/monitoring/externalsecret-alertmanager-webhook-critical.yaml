apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: alertmanager-discord-webhook-critical
  namespace: monitoring
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: SecretStore
    name: vault
  target:
    name: alertmanager-discord-webhook-critical
    creationPolicy: Owner
    deletionPolicy: Delete
  data:
    - secretKey: url
      remoteRef:
        key: clusters/mgmt-01/monitoring/alertmanager-webhook-critical
        property: url
        conversionStrategy: Default
        decodingStrategy: None
        metadataPolicy: None
