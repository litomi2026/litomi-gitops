resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 150m
    memory: 128Mi

serviceMonitor:
  enabled: false
  interval: 60s

config:
  outputs: |
    [OUTPUT]
        Name loki
        Match kube.*
        Host loki.mgmt.litomi.internal
        Port 3100
        Labels cluster=prod-01,job=fluent-bit
        Auto_Kubernetes_Labels On
        Line_Format json
        Retry_Limit False
