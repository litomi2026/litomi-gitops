apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: gtm-server-secret
  namespace: gtm-server
spec:
  refreshInterval: 5m
  secretStoreRef:
    kind: SecretStore
    name: vault
  target:
    name: gtm-server-secret
    creationPolicy: Owner
    deletionPolicy: Delete
  data:
    - secretKey: CONTAINER_CONFIG
      remoteRef:
        key: clusters/prod-01/gtm-server/gtm-server-secret
        property: CONTAINER_CONFIG
