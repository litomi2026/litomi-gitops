apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: vault
  namespace: cloudflared
spec:
  provider:
    vault:
      server: https://vault.mgmt.litomi.internal
      path: kv
      auth:
        kubernetes:
          mountPath: k8s-prod-01
          role: eso-cloudflared
          serviceAccountRef:
            name: eso-vault
            audiences:
              - vault
      caProvider:
        type: ConfigMap
        name: vault-ca
        key: ca.crt
